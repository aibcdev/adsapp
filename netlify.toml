[build]
  command = "npm ci && npm run build --workspace=@aibc/shared && npm run build --workspace=@aibc/portal"
  publish = "packages/portal/dist"

[build.environment]
  NODE_VERSION = "22"

# SPA routing (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
